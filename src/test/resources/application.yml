spring:
  application:
    name: product-service
  cloud:
    config:
      uri: http://localhost:9006
      profile: test
      label: main
  config:
    import: optional:configserver:http://localhost:9006
